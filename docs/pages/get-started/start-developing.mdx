// CashCompanion React Native App (Start mit Expo)
import React from 'react';
import { View, Text, TouchableOpacity, TextInput, ScrollView, StyleSheet } from 'react-native';

export default function App() {
  return (
    <ScrollView style={styles.container}>

      {/* Dashboard */}
      <View style={styles.card}>
        <Text style={styles.title}>🏠 Dashboard</Text>
        <Text style={styles.item}>Ausgaben: 450 € von 800 € Budget</Text>
        <Text style={styles.item}>Sparziel: "Urlaub in Spanien" – 300 € von 500 €</Text>
        <Text style={styles.item}>Aktive Challenge: Keine Lieferdienste (Tag 3/7)</Text>
        <TouchableOpacity style={styles.primaryButton}>
          <Text style={styles.buttonText}>➕ Ausgabe hinzufügen</Text>
        </TouchableOpacity>
      </View>

      {/* Ausgabe hinzufügen */}
      <View style={styles.card}>
        <Text style={styles.title}>➕ Ausgabe hinzufügen</Text>
        <TextInput style={styles.input} placeholder="Betrag (z. B. 15,90 €)" keyboardType="decimal-pad" />
        <TextInput style={styles.input} placeholder="Kategorie (Essen, Freizeit…)" />
        <TextInput style={styles.input} placeholder="Beschreibung (optional)" />
        <TouchableOpacity style={styles.successButton}>
          <Text style={styles.buttonText}>Speichern</Text>
        </TouchableOpacity>
      </View>

      {/* Statistik */}
      <View style={styles.card}>
        <Text style={styles.title}>📊 Statistik</Text>
        <Text style={styles.item}>• Kreisdiagramm (Platzhalter)</Text>
        <Text style={styles.item}>• Filter: Woche / Monat / Jahr</Text>
        <Text style={styles.item}>• Vergleich zum Vormonat</Text>
      </View>

      {/* Sparziel */}
      <View style={styles.card}>
        <Text style={styles.title}>🎯 Sparziel</Text>
        <Text style={styles.item}>Ziel: "Urlaub" – 300 € von 500 €</Text>
        <TouchableOpacity style={styles.primaryButton}>
          <Text style={styles.buttonText}>+ Betrag hinzufügen</Text>
        </TouchableOpacity>
      </View>

      {/* Challenges */}
      <View style={styles.card}>
        <Text style={styles.title}>🏆 Challenges</Text>
        <Text style={styles.item}>• Kein Coffee-to-go (3/7)</Text>
        <Text style={styles.item}>• 3 Tage nichts bestellen (aktiv)</Text>
        <Text style={styles.item}>• Belohnungen: Badges, Fortschritt</Text>
      </View>

    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 20,
    backgroundColor: '#f9fafb'
  },
  card: {
    backgroundColor: '#ffffff',
    padding: 20,
    marginBottom: 20,
    borderRadius: 16,
    shadowColor: '#000',
    shadowOpacity: 0.05,
    shadowRadius: 10,
    elevation: 2,
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 12,
    color: '#111827'
  },
  item: {
    fontSize: 16,
    color: '#374151',
    marginBottom: 4
  },
  input: {
    borderWidth: 1,
    borderColor: '#e5e7eb',
    padding: 12,
    borderRadius: 10,
    marginBottom: 10,
    backgroundColor: '#fff'
  },
  primaryButton: {
    backgroundColor: '#2563eb',
    padding: 12,
    borderRadius: 12,
    alignItems: 'center',
    marginTop: 10
  },
  successButton: {
    backgroundColor: '#22c55e',
    padding: 12,
    borderRadius: 12,
    alignItems: 'center',
    marginTop: 10
  },
  buttonText: {
    color: '#ffffff',
    fontWeight: 'bold'
  }
});

